{{yield (hash
  min=min
  max=max
  center=center
  lessons=lessons
  dates=dates
  activate=(action activate)
  deactivate=(action deactivate)
  day=(component dayComponent)
  dayIsDisabled=isDateOutOfRange
)}}
