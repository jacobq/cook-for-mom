import Component from '@ember/component';

export default Component.extend({
  classNames: ['parallax-container'],

  fluidClass: 'parallax-container-fluid',
  fluidComponent: 'parallax-container/fluid'
});
